# EasyPanel — Lara Beauty Store (Next.js)

| Setting | Value |
|---------|--------|
| Repository | `lara-beauty-store-gcc/laragccbackend` |
| Branch | `frontend` |
| Build Path | `/` (repo root — Dockerfile at root) |
| Dockerfile | `Dockerfile` |
| Proxy port | **3000** |

Synced from `main` @ `2478757b4d5c0897725084823b50b1060097b30c` (folder `frontend/` on main).

## إلا Deploy فشل

- **Build Path لازم يكون `/`** — ماشي `backend`
- **Branch لازم يكون `frontend`** — ماشي `main`
- **Proxy port = 3000**
- من بعد push جديد: EasyPanel → **Deploy** (أو Redeploy)
